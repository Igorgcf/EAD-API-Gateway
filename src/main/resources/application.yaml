spring:
  application:
    name: ead-api-gateway
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        serviceId: ead-config-service
      username: ${CONFIG_SERVER_USERNAME}
      password: ${CONFIG_SERVER_PASSWORD}
  profiles:
    active: dev
